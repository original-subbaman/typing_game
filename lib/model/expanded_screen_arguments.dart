import 'dart:ui';

class ExpandedScreenArguments {
  String titleText;
  Color titleTextColor;
  String bestValueText;
  String valueText;
  Color valueTextColor;
  Color bgColor;

  ExpandedScreenArguments(
      this.titleText, this.titleTextColor, this.valueText, this.valueTextColor,
      this.bgColor, this.bestValueText);

}
